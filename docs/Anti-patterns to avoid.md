Do not:
create a huge shared utils folder with no ownership
scatter domain logic across handlers and workers
mix connector-specific logic into Knowledge Core
hide policy logic in frontend code
let docs drift from repo structure
turn packages/shared into an accidental second monolith
over-nest folders before the repo has real scale
