package infra

// Postgres audit repository implementations will live here, implementing domain ports
// with explicit SQL separate from transport. Today the application service delegates to audit.Service.
