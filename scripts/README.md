# Scripts

| Script | Purpose |
|--------|---------|
| [smoke-local.sh](smoke-local.sh) | Quick HTTP smoke against a running API (`API_BASE`, `curl`). See [docs/STAGING_SMOKE_TEST.md](../docs/STAGING_SMOKE_TEST.md). |

Add new scripts with a one-line comment at the top of the script and update this table.
